import { TaskListNotApprovedComponent } from './task-list-not-approved.component'

describe('TaskListNotApprovedComponent', () => {
  it('should mount', () => {
    cy.mount(TaskListNotApprovedComponent)
  })
})